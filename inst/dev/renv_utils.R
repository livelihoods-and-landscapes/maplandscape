# snapshop current state of package dependencies in the lockfile
renv::snapshot()

# updage packages
renv::update()

# snapshop current state of package dependencies in the lockfile after updating packages
renv::snapshot()
